import { DashboardHeader } from "@/styles/dashboard.styles";
import React from "react";

const Header = () => {
  return (
    <div>
      <DashboardHeader> Es Dashboard</DashboardHeader>
    </div>
  );
};

export default Header;
